# Copy to .env when running from infra/docker/
# Host directory that contains best.pt (mounted read-only at /models in the container)

MODEL_DIR=../../application/models

# Example: SageMaker job output after infra/scripts/fetch_best_pt.*
# MODEL_DIR=../../runs/sagemaker/my-job
