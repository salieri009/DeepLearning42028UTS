# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
# Environment files can contain secrets (API keys, tokens, endpoints)
.env
.venv/
pip-log.txt
pip-delete-this-directory.txt
*.egg-info/
.eggs/
dist/
*.egg
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
.coverage.*

# Jupyter Notebook
.ipynb_checkpoints

# IDE / OS
.vscode/
.idea/
.cursor/
.DS_Store
Thumbs.db
desktop.ini

# Local agent / worktree tooling (not part of the product)
.claude/
.worktrees/

# Project Data & Models
/data/raw/*
!/data/raw/.gitkeep

# Training / ML (large local artifacts under train/)
train/runs/
train/scratch/

# Legacy monolith directory (local data / DVC tmp only — not part of new layout)
PROJECTS/CrowdNav/
/data/processed/*
!/data/processed/.gitkeep
/models/*
!/models/.gitkeep

# Logs and Checkpoints
*.log
*.ckpt
*.pt
*.pth
*.h5
*.hdf5

# Training / experiment outputs (large)
/runs/


# Node.js and React Native
# Dependencies (large, reproducible from lockfile)
node_modules/

# Frontend production bundle (generated by Vite)
application/frontend/dist/

# Java / Gradle (backend build outputs)
**/.gradle/
**/build/

# Expo local state/build outputs (generated)
.expo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
android/app/build/
android/.gradle/
android/app/build/
ios/Pods/
ios/build/
*.xcworkspace

# AWS / Cloud credentials
.aws/
credentials.json
aws_config.json

