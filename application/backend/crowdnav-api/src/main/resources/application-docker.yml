# Docker profile — activated via SPRING_PROFILES_ACTIVE=docker in compose/Dockerfile.
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://db:5432/crowdnav}
    username: ${DB_USER:crowdnav}
    password: ${DB_PASSWORD:crowdnav}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost}
